/**
 * 朋友小规模共用时：把你们部署好的「通用代理」地址写在这里，
 * 大家拉同一份前端即可，无需每人再填 URL。
 * DeepSeek 密钥只放在 proxy-server 的服务器环境变量里，不要写进本文件。
 *
 * 若仓库会公开，不要在此填写 FRIEND_TOKEN；可只在浏览器会话里填。
 */
window.SKILL_GOMOKU_CONFIG = {
  defaultProxyUrl: "",
  defaultProxyToken: ""
};
